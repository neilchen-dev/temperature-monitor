# 复制到 Home Assistant 的 secrets.yaml，并替换成至少 32 字节随机值。
temperature_monitor_history_api_key: replace-with-a-long-random-secret
# 可选：服务端设置 TEMPERATURE_API_KEY 后，rest_command 中启用对应 header。
temperature_monitor_temperature_api_key: replace-with-a-long-random-secret
